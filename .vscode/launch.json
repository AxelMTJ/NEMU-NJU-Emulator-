{
    "configurations":[
{    "name": "Debug",
    "type": "gdb",
    "request": "launch",
    "program": "${workspaceFolder}/nemu", // Update this path to your executable
    "args": [], // Optional: Command-line arguments for your program
    "stopAtEntry": false,
    "cwd": "${workspaceFolder}",
    "MIMode": "gdb",
    "setupCommands": [
        {
            "description": "Enable pretty-printing for gdb",
            "text": "-enable-pretty-printing",
            "ignoreFailures": true
        }
    ]}
]
}
